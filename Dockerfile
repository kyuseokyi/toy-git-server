#생성할 도커 이미지의 베이스 이미지를 지정ㅎ나다.
FROM golang:1.9

#컨테이너 안에서 실행할 명령을 정의하는 인스트럭션.
RUN mkdir /echo

#host의 파일 또는 디렉토리를 컨테이너 안으로 복사하는 인스트럭션인다.
COPY main.go /echo

#컨테이너 실행시 컨테이너 안에서 실행할 프로세스를 지정한다. RUN 명령어는 이미지 빌드시 한번 실행되며 CMD명령어는 컨테이너를 시작할때 한번 실행된다.
CMD ["go", "run", "/echo/main.go"]


